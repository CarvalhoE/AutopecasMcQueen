<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <select id="users">
        <%
        for ( var i = 0; i < values.length; i++ )
        {
            %><option value="<%=values[i].ID_Funcionario%>"><%= values[i].DS_Login%></option><%
        }
        %>
    </select>

    <table style="border: solid 1px black;">
        <tr>
            <th>ID</th>
            <th>Nome</th>
            <th>Login</th>
        </tr>
        <%for (var i = 0; i < values.length; i++) {%>
            <tr>
                <td><%=values[i].ID_Funcionario%></td>
                <td><%=values[i].NM_Nome%></td>
                <td><%=values[i].DS_Login%></td>
            </tr>
        <% } %>
    </table>
</body>

</html>